services:
  postgres-inventory:
    image: postgres:14
    container_name: postgres-plan
    hostname: postgres-plan
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: plan_db